# Magic Patterns - Vite Template

This code was generated by [Magic Patterns](https://magicpatterns.com) for this design: [Source Design](https://www.magicpatterns.com/c/3ceftdxzy9fg3pvpgxkbqd)

## Getting Started

1. Run `npm install`
2. Run `npm run dev`

## TODO

- Live Google reviews per outlet via Google Places API (Place Details `reviews` field). Needs a server-side proxy (app is a pure Vite SPA, no backend yet) — key can't ship in client bundle. Requires Place ID lookup per outlet first. Revisit backend target (Vercel fn / Netlify fn / Node server) before implementing.
